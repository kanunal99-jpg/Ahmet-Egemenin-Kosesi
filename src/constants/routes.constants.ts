export const ROUTES = {
  HOME: '/',
  CATEGORY: '/category/:slug',
  FAVORITES: '/favorites',
  HISTORY: '/history',
  PARENT_PANEL: '/parent-panel',
  LOGIN: '/login',
  REGISTER: '/register',
  NOT_FOUND: '*',
} as const;
